%h1 Market Opportunities

%table
  %thead
    %tr
      %th Icon
      %th Name
      %th Best Sale Price
      %th Best Buy Price
      %th Opportunity
      %th Good/Bad Ratio

  %tbody
    - @items.each do |item|
      %tr
        %td
          %img{ src: item.icon }
        %td= link_to item.name, item
        %td= number_with_delimiter item.best_selling_price
        %td= number_with_delimiter item.best_buying_price
        %td= number_with_delimiter item.market_opportunity
        %td
          = item.selling_below_market_value.sum(:quantity)
          = "/"
          = item.buying_above_market_value.sum(:quantity)
